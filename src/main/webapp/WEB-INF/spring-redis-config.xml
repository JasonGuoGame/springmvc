<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

       <bean id="jedisConnectionFactory"
             class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory"
             p:hostName="localhost"  p:port="6379" p:database="1" p:password=""/>
       <!--<bean id="lettuceConnectionFactory"
             class="org.springframework.data.redis.connection.lettuce.LettuceConnectionFactory"
             p:hostName="scnyig.modelmanager.sashq-d.openstack.sas.com"
             p:port="6379"/>-->
    <!--Use Redis Token Store-->
    <!--<bean id="tokenStore"
                class="org.springframework.security.oauth2.provider.token.store.redis.RedisTokenStore">
        <constructor-arg name="connectionFactory" ref="redisConnectionFactory0"/>
    </bean>

    &lt;!&ndash;create redis connection factory and set db 1&ndash;&gt;
    <bean id="redisConnectionFactory0"
                class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory"
          p:hostName="localhost" p:port="6379" p:password="" p:database="1" p:useSsl="false">
    </bean>-->
</beans>

